language: go
before_install:                                                                 
  - apt-get update
  - apt-get install alsa
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh  
  - dep ensure
go:
  - "1.10"
